language: python

python:
  - "2.7"

install:
  - pip install -r requirements.txt --use-mirrors
  - pip install -r dev_requirements.txt --use-mirrors
  - pip install coveralls --use-mirrors

script: make package BINROOT=

after_success:
  coveralls

notifications:
  slack: momentum-labs:qdIanVeSHF2oFXDY0GbNxofo#automata
